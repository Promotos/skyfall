<%= link_to t("note.new_note"), notes_new_path %>
<p>
<table id="sky_table">
	<tr>
		<th align="left">
			Id
		</th>
		<th align="left">
			Title
		</th>
	</tr>
	<% @notes.each do |note| %>
	<tr>
		<td><%= link_to note.id, notes_show_path(:id=>note.id) %></td>		
		<td> <%= note.note_id %> </td>
	</tr>
	<% end %>
</table>
</p>